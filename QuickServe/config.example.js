// Public runtime configuration for QuickServe (copy to config.js and edit)
// This file is loaded by index.html before services to configure API base URL at runtime.

window.ENV = {
  // Set your backend base URL (e.g., 'https://api.yourdomain.com/v1')
  API_BASE_URL: 'https://api.example.com/v1'
};

